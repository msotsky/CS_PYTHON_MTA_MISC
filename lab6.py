# COMP 1631 Lab 6 Fall 2018
# Filename: lab6.py
# Names:
# Problem Description:


def mult(n,m):
    ''' returns the product of n and m
        note that the multiplier n must be
          a non-negative inter value  '''
    product = 0
    for i in range(n):
        product = product + m
    return product


def dot(k,l):
    result = 0
    if len(K) != len(L) or len(L) == 0 and len(K)== 0:
        return 0.0
    else:
        for x in range(k) and z in range(l)
            result += (x * z)
        return result
